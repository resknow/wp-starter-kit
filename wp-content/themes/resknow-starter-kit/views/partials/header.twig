{% from "macros/menu/macro.twig" import menu %}
{% from "macros/mobile-menu/macro.twig" import mobileMenu %}

<header class="px-8 py-4">
    <div class="container flex justify-between items-center">
        <a class="size-lg weight-bold" href="/">{{ timber.site.name }}</a>

        {{ mobileMenu(menu, globals.phone ?? "0208 300 8833") }}
        {{ menu(menu) }}
    </div>
</header>